// Log helpers

`define LS_Log(msg) `Log(`msg,, 'LivingSpace')

// Easy consts/singletons

`define LS_DEFAULT_EVENT_PRIORITY class'LS_DataStructures'.const.DEFAULT_EVENT_PRIORITY
`define LSDATA class'XComGameState_LivingSpaceData'.static.GetSingleton()

// Gamestate management helpers

// X2MBC-Redefine
`define SubmitGameState(state) `XCOMGAME.GameRuleset.SubmitGameState(`state);

// X2MBC-Redefine
`define CleanupGameState(state) `XCOMHISTORY.CleanupPendingGameState(`state);
